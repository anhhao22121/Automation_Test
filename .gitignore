# --- Python Core ---
__pycache__/
*.py[cod]
*$py.class

# --- Virtual Environments (Môi trường ảo) ---
# Rất quan trọng: Không bao giờ commit thư mục venv
.venv/
venv/
env/
ENV/

# --- Environment Variables (Bảo mật) ---
# Chứa user/pass/url test, tuyệt đối không commit
.env
.env.test
.env.local

# --- Selenium & Browser Drivers ---
# File log sinh ra khi chạy driver
geckodriver.log
chromedriver.log
msedgedriver.log
npm-debug.log

# Driver binary (nếu bạn tải về để trong project thay vì dùng webdriver-manager)
drivers/
*.exe
*.driver

# --- Test Artifacts (Kết quả chạy test) ---
# Ảnh chụp màn hình khi test fail
screenshots/
*.png
*.jpg

# Báo cáo kết quả test (HTML Reports, XML)
reports/
test-output/
htmlcov/
site/
.tox/
.nox/
.coverage
.coverage.*
nosetests.xml
coverage.xml
*.cover
.pytest_cache/

# Nếu dùng Allure Report
allure-results/
allure-report/

# --- IDEs & Editors ---
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# --- OS Files ---
.DS_Store
Thumbs.db

# --- Dependency Management ---
# LƯU Ý: Với dự án Testing, nên COMMIT file lock để đảm bảo môi trường đồng nhất
# Pipfile.lock  <-- Đã bỏ comment (Nên commit file này)
# poetry.lock   <-- Nên commit file này nếu dùng Poetry